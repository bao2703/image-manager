@model Album

@{
    ViewData["Title"] = $"{Model.Name} Album";
}

<div class="mt-2">
    <div class="row px-1 justify-content-center">
        <div class="col-md-12">
            <h1>@Model.Name</h1>
            <div>
                <i class="fa fa-tag"></i> <a href="#">Ecchi</a> &nbsp;
                <i class="fa fa-eye"></i> 26401 Views &nbsp;
                <i class="fa fa-image"></i> @Model.Images.Count Images &nbsp;
                <i class="fa fa-calendar"></i> @Model.CreatedDate?.ToString("d MMM yyyy") &nbsp;
                <i class="fa fa-user"></i> by <a href="#">@Model.User.Name</a>
            </div>
            <p>
                @Model.Description
            </p>
        </div>
    </div>
    <div class="row px-1 mb-2">
        <div class="col-md-12">
            <a asp-action="Create" asp-controller="Image" class="btn btn-primary">
                <i class="fa fa-plus"></i>
                Add
            </a>
            @await Html.PartialAsync("_EditPartial")
        </div>
    </div>
    <div class="row no-gutters">
        @foreach (var item in Model.Images)
        {
            <div class="col-6 col-md-3 col-lg-2 p-1">
                @await Html.PartialAsync("_ImagePartial", item)
            </div>
        }
    </div>
</div>

@section Scripts {
    <script id="script-edit" src="~/js/edit.js" asp-append-version="true" data-url="@Url.Action("Delete", "Image")"></script>
}